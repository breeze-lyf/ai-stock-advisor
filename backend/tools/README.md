# Backend Tools (Compatibility)

This directory is kept for backward compatibility.

- `tools/diagnostics/*.py` are wrappers now.
- Active diagnostic scripts live under `scripts/dev/diagnostics/`.

Prefer running scripts from `backend/scripts/README.md` paths.
